# Backend API Gateway

This directory contains the core Node.js/TypeScript backend services for Yusra Engineering Ai.
In development, it normally exposes APIs at `http://localhost:4000/api/v1` for the frontend to consume.
